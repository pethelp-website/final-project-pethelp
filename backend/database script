drop table if exists usertype;
drop table if exists users;
drop table if exists pet_report;
drop table if exists shelter;
create table users(
   id SERIAL primary key,
   name     VARCHAR(30) not null,
   email    VARCHAR(120) not null,
   address  VARCHAR(120),
   city     VARCHAR(30) not null,
   postcode VARCHAR(12),
   password VARCHAR(250) not null
  );


insert into users (name, email, address, city, postcode, password) values ('usman','abc@gmail.com','corts catalanes','Barcelona','b089','pass1');
insert into users (name, email, address, city, postcode, password) values ('Gloria','efg@gmail.com','calle valencia','Barcelona','b020','pass1');
insert into users (name, email, address, city, postcode, password) values ('Gabi','hij@gmail.com','calle montaner','valencia','a89','pass1');
insert into users (name, email, address, city, postcode, password) values ('isha','klm@gmail.com','calle rocafort','Barcelona','b020','pass1');
insert into users (name, email, address, city, postcode, password) values ('alisa','xyz@gmail.com','calle taragona','girona','f30','pass1');



create table usertype (
 id SERIAL primary key,
 user_id      INT REFERENCES users(id),
 type  VARCHAR(30) not null,
 display_type  VARCHAR(30) not null
);


create table shelter (
id SERIAL     primary key,
name 		  VARCHAR(30) not null,
location      VARCHAR(120)
);

create table pet_report (
	id SERIAL    primary key,
	user_id      INT REFERENCES users(id),
	shelter_id 	 INT REFERENCES shelter(id),
	race         VARCHAR(30) not null,
	color        VARCHAR(30) not null,
	image 		 bytea,
	type 		 VARCHAR(15)
);



select * from shelter;
